# Endurece el entorno de desarrollo para mitigar advisories de webpack-dev-server
# Solo escucha en loopback (no accesible desde la red)
HOST=127.0.0.1
PORT=3000

# Asegura que NO se deshabilite el host check (variable histórica de CRA)
DANGEROUSLY_DISABLE_HOST_CHECK=false

# Opcional: fuerza el cliente a usar loopback para el websocket
# (algunos navegadores pueden reescribir el host si no se fija)
WDS_SOCKET_HOST=127.0.0.1
WDS_SOCKET_PORT=3000
